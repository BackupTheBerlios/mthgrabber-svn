#ifndef _SWITCHBOARD_H_
#define _SWITCHBOARD_H_

#include <wx/wx.h>

class	switchBoard : public wxFrame
{
public:
  switchBoard(const wxString& title, const wxPoint& pos, const wxSize& size);
};

#endif // _SWITCHBOARD_H_
